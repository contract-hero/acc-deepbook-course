node_modules/
.vitest-cache/
.DS_Store
*.log

# Personal session memory (not part of the project artifact)
.remember/

# Per-project course state (created by the MCP server when this repo is itself
# used as a projectRoot during local testing).
.sui-deepbook-course/

