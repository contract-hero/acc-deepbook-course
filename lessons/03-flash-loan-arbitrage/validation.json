{
  "ran_at": "2026-06-01T16:23:46Z",
  "offline_build": {
    "passed": true,
    "command": "pnpm install && pnpm build",
    "note": "tsc -b typecheck + vite production build of the seeded reference-app's TypeScript; 422 modules transformed, no errors. Confirms the TS seed compiles. The Move package under contracts/ was NOT built or published \u2014 that requires the deepbook-sandbox + sui CLI (and the .external-packages/{token,deepbook} source deps), which is out of scope for the offline soundness check."
  },
  "known_issues": [
    "This lesson's arb_executor Move package must be published before the live test: `DEEPBOOK_SANDBOX_DIR=$HOME/workspace/deepbook-sandbox/sandbox bash contracts/scripts/deploy.sh` (writes the chain-specific id into deployment.json). The committed deployment.json carries a placeholder id; re-publish after any sandbox re-genesis.",
    "deploy.sh is first-publish-only: re-running it against the SAME sandbox genesis fails because `sui client test-publish --pubfile-path` already recorded the package. A learner publishes once per genesis (works); re-genesis (pnpm down + deploy-all) regenerates a clean pubfile. Minor re-runnability wart, not learner-path-blocking."
  ],
  "live_gate": {
    "status": "passed",
    "ran_at": "2026-06-02T00:30:00Z",
    "command": "pnpm vitest run (arb_executor Move package published first via contracts/scripts/deploy.sh)",
    "sandbox": "local deepbook-sandbox @ origin/main e62fa7d (pnpm deploy-all --quick); DEEP_SUI + SUI_USDC pools",
    "result": "2/2 live tests passed reliably across repeated runs (8/8 then 3/3) \u2014 borrow\u2192execute_base\u2192repay in one PTB, and short-repay reverts inside arb_executor::execute_base. Flakiness fixed: seedPoolBaseLiquidity now escrows base into vault.base_balance by resting a POST_ONLY DEEP ask (a BalanceManager deposit alone does not feed the vault)."
  },
  "learner_validation": {
    "status": "passed",
    "ran_at": "2026-06-02T00:50:00Z",
    "method": "Learner-reconstruction: a sub-agent implemented src/flashLoan.ts from the section prompts alone (reference solution stubbed out, tests provided only for the interface), then ran the live gate against the sandbox.",
    "result": "2/2 live tests passed (borrow\u2192execute_base\u2192repay; short-repay revert). The learner independently rebuilt seedPoolBaseLiquidity with the resting-ask vault-escrow \u2014 confirming the corrected section-1 teaching works.",
    "note": "Single from-scratch learner walkthrough \u2014 stronger than the conductor's key-moment blanking (the whole core module was rebuilt from prose). The dual output-style (learning vs explanatory) variation only affects narration, not the resulting code."
  }
}
