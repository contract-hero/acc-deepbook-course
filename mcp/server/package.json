{
  "name": "@sui-deepbook-course/mcp-server",
  "version": "1.0.0",
  "description": "MCP server for Sui DeepBook course",
  "type": "module",
  "engines": {
    "node": ">=18"
  },
  "main": "dist/index.js",
  "scripts": {
    "build": "rm -rf dist && node esbuild.config.mjs",
    "build:tsc": "tsc",
    "clean": "rm -rf dist",
    "start": "node dist/index.js",
    "dev": "tsx src/index.ts"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "zod": "^3.22.0"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "esbuild": "^0.24.0",
    "typescript": "^5.4.0",
    "tsx": "^4.7.0"
  }
}
